version: '3.8'

services:
  app:
    image: node:18-alpine
    working_dir: /app
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
    command: sh -c "npm install && npm run start"

volumes:
  pgdata:
